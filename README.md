# SSI Auth Middleware Testing

Running tests:

1. Start the middleware according to the explanation in `middleware/README.md`.
2. Start the client application and testing script according to the explanation in `load-test/README.md`.
